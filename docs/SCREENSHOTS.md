# Screenshot / capture guide

Capture after `feat/contract-governance-demo` is on production (or via `npm run dev`).

## Required shots (no PII)

1. **Conflict case** — `/cases/conversion-conflict` full viewport (two definitions + resolution)
2. **Library** — `/metrics` with five demos visible
3. **Resolved contract** — `/metrics/taxa-de-conversao` showing score + approval panel
4. **Version diff** — detail page with history selected and diff entries
5. **Export actions** — Markdown + dbt YAML buttons visible

## Suggested filenames

```text
assets/screenshots/07-conversion-conflict.png
assets/screenshots/08-approval-and-versions.png
assets/screenshots/09-dbt-yaml-export.png
```

## Social preview

Keep `assets/social-preview.png` (1280×640). Re-upload in GitHub → Settings → Social preview after refresh.
